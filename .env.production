# Production environment variables for Netlify deployment

# Node environment
NODE_ENV=production

# API URL for production - empty for same-origin requests to Netlify Functions
# This allows the frontend to make API calls to the same domain
VITE_API_URL=

# MongoDB connection (not used in frontend-only deployment)
# MONGODB_URI=your-production-mongodb-uri