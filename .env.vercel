# Vercel Environment Configuration
NODE_ENV=production

# API Configuration for Vercel
# Empty VITE_API_URL means API calls will be same-origin to Vercel Functions
VITE_API_URL=

# Database Configuration
# MongoDB connection not used in frontend-only deployment
# Backend API handles database connections via Vercel Functions
MONGODB_URI=

# Vercel-specific settings
VERCEL=1
VERCEL_ENV=production

# Build Configuration
BUILD_COMMAND=npm run build
OUTPUT_DIRECTORY=dist

# Function Configuration
FUNCTIONS_SRC=server
FUNCTIONS_DIST=.vercel/output/functions